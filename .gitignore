# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
# 任意层级的构建输出(android/build、windows build 等),防止 git add -A 误入库
**/build/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds

# Android
**/android/**/gradle-wrapper.jar
.gradle/
**/android/captures/
**/android/gradlew
**/android/gradlew.bat
**/android/local.properties
**/android/**/GeneratedPluginRegistrant.java
**/android/key.properties
*.jks
**/android/app/debug/
**/android/app/profile/
**/android/app/release/

# Local config / secrets
.env
*.key

# Local preview artifacts (golden PNGs generated for manual review)
/outputs/

.workbuddy/

# gen-l10n 产物(由 CI 的 flutter pub get / gen-l10n 生成)
/lib/l10n/generated/

# tool/ 下 C++ 排查钩子的编译中间产物(源码 msg_hook.cpp 入库，产物不入库)
# 由 cl.exe /link /DLL 生成，随工具链与架构而变，无版本管理价值
/tool/*.dll
/tool/*.exp
/tool/*.lib
/tool/*.obj
# 上述工具运行时吐出的采样结果快照(临时数据，非源文件)
/tool/msg_hist_result.txt
/tool/msg_hist_storm.txt
