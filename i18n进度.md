# i18n / l10n 国际化进度

> MusicFlow-client（Flutter）的 i18n/l10n 全量迁移任务。语言方向：**中文默认 + English**，支持跟随系统/中文/English 三档切换（已接入）。

## 整体任务需求

- **基础设施**（已完成）：`l10n.yaml`、`app_zh.arb` / `app_en.arb`、`flutter gen-l10n` 生成、`AppLocalizations.of(context)` 访问模式、`l10nNow(pref)`（无 BuildContext 环境）、语言偏好设置持久化。
- **全量迁移**（~4300 处，183/225 文件）：按批逐簇将 `widgets/` → `features/` → `core/` → `data/` → `providers/` 的硬编码中文替换为 `loc.xxx`。
- **CI 守卫**（`tool/check-l10n.mjs`）：
  - ARB 键对齐（zh/en 键集合完全一致）；
  - 硬编码中文扫描（排除 `l10n/` 与 `l10n/generated/`，默认 `--gate-cjk` **未启用**，不阻塞 CI）；
  - providers 误用 `AppLocalizations.of(context)` 检查（必须走 `l10nNow`）。
- **提交策略**：`lib/l10n/generated/` 强制纳入版本库（`git add -f`），确保不跑 `gen-l10n` 的 CI workflow（build-android/ui-guard 等）可直接编译。

## 当前进度（全量迁移完成,硬门禁已启用）

| 批次 | 范围 | 文件数 | 新增 ARB 键 | 提交 | CI |
|---|---|---|---|---|---|
| 基础设施 | l10n.yaml、ARB、语言偏好、app.dart 接线 | — | 9 | `8ca9d65` | ✅ |
| **Batch A** | 顶层 `lib/widgets/**` | 12 | 71 | `a149bbf` | ✅ l10n guard + test 绿灯 |
| **Batch B** | `features/settings/**` | 8 | 162 | `2fb48c8` | ✅ |
| **Batch C** | `features/discover/**` + search_page | 4 | 62 | `abf7f68` | ✅ |
| **Batch D1** | `features/library/**` | 22 | 250 | `1057ab3` | —
| **Batch D2** | `features/player/**` | 7 | 136 | `549e5b0` | —
| **Batch D3** | `features/auth/**` + `features/search/**` | 8 | 57 | `e0e3334` | —
| **Batch E** | `core/**` 全量迁移（UI→loc / 调试日志→英文） | — | 15 | `a3d0301` | —
| **Batch F** | `data/**` + `features/settings/services` | ~53 | 27 | `8805439` | ✅ |
| **Batch G** | `providers/**`（收尾,全 `l10nNow`） | ~7 | 19 | `ba6a92d` | 盯CI中 |
| 收尾 | 启用 `--gate-cjk` 硬门禁 + `player_hero_helpers` 正则去CJK化 | 1+CI | 0 | `c27a517` | — |

**累积 ARB 共享键：832**（zh/en 各 832）。全量迁移完成（widgets/features/core/data/providers 全部走 `loc` / `l10nNow`，lib/features 与 lib/providers 均无硬编码中文 UI 文案）。原 player_hero_helpers.dart 1 处功能性分隔符正则已改为 CJK-free 码位转义（`[\s\u{00B7}\u{30FB}\u{2022}]`），`--gate-cjk` 硬门禁已在 CI 强制启用。

## 关键约定

- 每个 `build`/闭包内捕获一次 `final loc = AppLocalizations.of(context);`，子树文案统一走 `loc.xxx`。
- 含 `{placeholder}` 的键必须补 `@key` 的 `description` 与 `placeholders`（含 `type`），生成后为按声明顺序的位置参数方法。
- 含硬编码中文的 `const` 控件/表达式必须去掉 `const`（值来自 build 期 `loc`）。
- 无 BuildContext 的纯函数/工具：优先由调用方 build 期传 `loc` 参数；确无 context 且无法传参才用 `l10nNow(ref.read(appLanguageProvider).preference)`。

## 本地工具链

- Flutter 3.47.1 / Dart 3.13.1（`C:\Users\ray5378\.workbuddy\binaries\flutter`）
- 每批流程：`flutter gen-l10n` → `dart analyze <dir> lib/l10n/generated` → `node tool/check-l10n.mjs` → `git add -f lib/l10n/generated` → push → 盯 CI

## 关联仓库

- 主仓库 i18n 见 `MusicFlow/i18n进度.md`（✅ 已完成，随 v2.0.0 发版）。
- 插件仓库 i18n 见 `MusicFlow-plugins/i18n进度.md`。
