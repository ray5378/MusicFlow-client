# MusicFlow-client 代码审计报告（v3 · 三轮收敛版）

- v1 初版（5 路并行）→ v2 复核修订（推翻 3 误报/降级 6 条）→ **v3 第三轮**：一路"证伪组"
  独立验证 v2 全部存活结论 + 两路扫描前两轮盲区（Windows 原生/音频引擎、链路 A/传输安全）。
- 基线：`bc44e4e`（main）· `flutter analyze` 53 issues / **0 error**
- 威胁模型：单用户自建服务端（家庭 LAN）、Windows 桌面为主 + Android。

---

## 一、v1 → v2 修正对照（最重要的部分）

| v1 编号 | v1 结论 | v2 复核结论 | 修正依据 |
|---|---|---|---|
| #5 「顺序播放=列表循环 bug」🟠 | **撤回（误报）** | 本机 UI 播放模式**只有三态**：`cyclePlaybackMode()`（player_provider:2477-2486）随机→列表循环→单曲循环，**不存在「顺序播放(order)」态**；且 `repeatAll` 底层故意用 `LoopMode.off`（player_provider:2444-2457 注释："队列切歌由外层状态机驱动，Repeat All 用 LoopMode.off"）。`next()` 队尾回绕 + `_onSongCompleted:3024` 播完续播 = **列表循环的正确行为**。`order` 态只存在于投屏链路（full_player_page:1277、mini_player:1088）。原报告把 `LoopMode.off` 误解为"顺序播放"，前提不成立 → 撤回 |
| #11 SSDP start/stop 竞态 🟠 | **撤回（无触发路径）** | `SsdpDiscovery` 仅由 `DlnaManager` 持有（dlna_manager:17），manager 是全局单例 provider（dlna_provider:125-129），全仓**无任何 `stopListening()` 调用点**，start/stop 不会重叠 → 竞态不可达 |
| #13 歌词页 dispose 不完整 🟡 | **撤回（建议不可行）** | 核查 scrollable_positioned_list 0.2.3 源码：`ItemScrollController` **无公开 dispose**（仅有私有 `_detach`）；`ItemPositionsListener` 为 abstract 且接口无 dispose，实现类 `ItemPositionsNotifier` 内部的 `ValueNotifier` 也未暴露 dispose。包内 `ScrollablePositionedListState.dispose`（包源码 296-302 行）**已自动 removeListener** → 不存在泄漏，且"调用二者 dispose"编译不过 |
| #1 凭据明文 🔴 | **维持问题，降级 🟠** | 复核属实：纯 drift（无 sqlcipher / secure storage），password/apiKey 明文入 SQLite，模型注释 `// Encrypted` 与实现相反。但按威胁模型校准：Windows 单用户桌面 + Android 应用私有目录，触发前提=攻击者已能读用户目录（此时系统内其他明文同样暴露）→ 🔴 偏高。**仍建议优先修**（安全债 + 注释误导），但以 🟠 表述 |
| #3 非原子写 🔴 | **维持问题，降级 🟠** | 缓存是可重下数据，损坏最坏=单曲播放失败/白占空间，非核心数据丢失 |
| #4 下载 tmp 路径穿越 🔴 | **维持问题，降级 🟠** | 属实（`offline_cache_daemon.dart:264` 用原始 `song.id`，同文件 `_safeName` 漏用）。但 song.id 来自**用户自建的可信服务端**，恶意触发需服务端已被攻破 → 低概率，属纵深防御 |
| #2 HttpClient 泄漏/挂起 🔴 | **维持问题，降级 🟠** | 属实（`device_description.dart:18-22`：`close` 不在 finally；读 body 无超时）。影响=单设备发现失败/延迟 + fd 缓慢增长，SSDP 周期扫描会累积 |
| #6 dlnaCast timer 泄漏 🟠 | **维持问题，降级 🟡** | 属实（无 onDispose、无 dispose 覆写），但 provider 为**全局非 autoDispose 单例**，仅 invalidate/热重载触发；正常路径（stopCast/detachOnAppRemoved）已清理 |
| #10 auth Dio 不 close 🟠 | **维持问题，降级 🟡** | 属实（4 处 `new Dio()` 无 close），但调用点仅登录页与编辑服务器页的**用户手动操作**（login_page:57、edit_library_page:310），低频且进程重启即清 |
| #12 sleepTimer 无 dispose 🟡 | **维持问题**（🟡） | 属实；同上，全局单例仅释放场景触发，防御性修复 |
| #7 FutureBuilder 反模式 🟠 | **维持（🟠），修正描述** | 属实（`aggregate_search_results.dart:141-150` build 内 `fetcher()` 新 Future + 无 `hasError`）。修正：使用方是 **album/artist/playlist/song 四个媒体库列表页**（非搜索页），触发场景=本地输入过滤时父级每次 rebuild 重发一次查询 + 失败被伪装成"无匹配" |
| #8 孤儿文件 🟠 | **维持（🟠）** | 复核 init()（offline_cache_manager:172-193）确认：只 `_loadIndex()` 从不扫描目录；索引 1s debounce（:121,212-218）且无退出 flush。成立 |
| #9 磁盘空间零校验 🟠 | **维持（🟡）** | 属增强项，降为防御性 🟡 更贴切（现有 catch 已防崩溃，只是可能留半文件 + 体验差） |

**v1 未收录、复核补录的条目**：
- `frozen_playback_provider.dart:22-27,41-45`：`build()` 内写可变字段 `_last`（副作用），靠"返回相同值去重"工作，语义脆弱 → 🟡（v1 收到子代理报告但漏写）。
- `effective_playback_provider.dart:22,40,51`：热点 provider `ref.watch` 了 `dlnaCastProvider`/`castPeerControllerProvider` **整状态**无 `.select`，任意字段变更触发重算 → 🟡（同上漏写）。

---

## 二、复核后的最终问题清单（去重、按修复价值排序）

### 🟠 值得修（真实可达、有实际影响）

| 位置 | 问题 | 触发场景 |
|---|---|---|
| `data/repositories/library_repository.dart:59-60,103-104` | Subsonic 凭据明文落库，注释 `// Encrypted` 误导 | 本地文件外泄/备份泄露；改 `flutter_secure_storage` 至少先修注释 |
| `core/dlna/device_description.dart:18-22` | `HttpClient.close` 不在 finally；读 body 无超时 → 连接泄漏 + 可永久挂起 | 设备"接受连接不回 body"时单次发现卡死，周期扫描 fd 累积 |
| `core/offline/offline_cache_manager.dart:259,303` | 非原子写（无 .part+rename） | 弱网/强退 → 缓存文件截断损坏 |
| `core/offline/offline_cache_manager.dart:172-193` | init 不扫描目录 + 索引 1s debounce 无退出 flush → 孤儿文件不可回收、磁盘增长 | 下载完成后 1s 内杀 app |
| `providers/offline/offline_cache_daemon.dart:264` | tmp 文件名用原始 song.id，未走 `_safeName` | 纵深防御（song.id 来自服务端） |
| `features/search/widgets/aggregate_search_results.dart:141-150` | build 内 `fetcher()` 新 Future + 无 hasError 分支 | 4 个媒体库页本地过滤每次 rebuild 重查；查询失败显示"无匹配" |

### 🟡 防御性/可维护性

- `providers/cast/dlna_provider.dart:303-306` + `providers/player/sleep_timer_provider.dart`：全局单例 provider 的 Timer 缺 dispose（仅 invalidate/热重载触发）。
- `data/repositories/auth_repository.dart:18,109,203,243`：临时 Dio 不 close（手动低频）。
- `providers/player/player_state.dart:102-110`：`hasNext/hasPrevious` 命名与语义粗糙（队列非空即真，与 loopMode 无关）；`core/dlna/dlna_manager.dart:959` 注释仍期待 `hasNext` 可能为 false（与投屏 order 语义的衔接点已断）→ 未来若加本地 order 态会踩坑，建议在 state 层补语义化判断并更新注释。
- `frozen_playback_provider.dart:22-27,41-45`：build 内写可变字段。
- `effective_playback_provider.dart:22,40,51`：热点 provider 无 `.select`。
- `core/offline/offline_cache_manager.dart:335/342/356/379`：读方法触 LRU（touch），读即打乱淘汰序。
- `core/network/fallback_interceptor.dart:14,80-118`：共享可变失败计数竞态；重试 mutate 共享 baseUrl。
- `core/network/health_checker.dart:27`：`_check()` 不 await，慢探测与下个 tick 重叠。
- 离线模块磁盘空间零校验（下载前 stat）。
- `widgets/cover_art_image.dart:162`：缺 `gaplessPlayback`（闪烁）。
- `data/sources/remote/gd_music_api_client.dart:223/288/317`：三处重复"候选源回退 + 静默 catch"循环，最终失败无日志。
- **死代码**：`StoragePermissionService`、`windowedSongsProvider`/`windowedAlbumsProvider`、`AzItem`、`RouteSelectionPage`。
- **8 个未使用依赖**：`cached_network_image` / `share_plus` / `wakelock_plus` / `marquee` / `animations` / `file_saver` / `cupertino_icons` / `just_audio_background`（lib+test+Manifest 全 0 引用，二次确认）。

### 测试缺口（维持 v1 结论）

离线编排（offline_provider + cache_daemon）、登录页（567 行）、app_database 层、covers/lyrics 多源、providers/library/* 均零测试；
auth_provider_test / offline_cache_manager_test / dlna_auto_next_sim_test 用固定 `Future.delayed`（flaky 源）。

---

## 三、维持"已排查无问题"的结论

- 证书校验无绕过、列表虚拟滚动/图片缓存/播放器竞态守卫/i18n/平台检测/启动兜底（runZonedGuarded）/拼音工具 均复核维持无问题。
- v1 已自查修正的两条仍然成立：`ConnectivityMonitor` stop→start（每次 provider 重建 new 实例，不复现）；
  `offline_cache_daemon` tmp.delete 在 catch 内（非泄漏）。

---

## 四、结论

v1 报告**方向正确但口径偏激进**：7.3 万行代码 0 error、且主要链路（播放器竞态、虚拟列表、图片、
错误处理、i18n）实测健壮。真实可修的问题集中在**离线缓存文件安全**（原子写/路径/孤儿）、
**DLNA HTTP 资源管理**、**凭据存储**与若干 provider 生命周期/副作用，无 🔴 级稳定复现崩溃。
建议按第二节 🟠 六条优先处理，其余按 🟡 批次消化。

---

# v3 · 第三轮（盲区扫描 + 独立证伪）

## 3.1 v2 存活结论的独立验证结果

| v2 条目 | 第三轮验证 |
|---|---|
| 凭据明文（SQLite） | ✅ 确认（`connection_native.dart:12` NativeDatabase 纯明文） |
| HttpClient 泄漏/挂起 | ✅ 确认（catch 块 27-30 无 close） |
| 非原子写 | ✅ 确认 |
| 孤儿文件 | ✅ 确认（init 不扫描 + 无退出 flush） |
| tmp 路径穿越 | ✅ 确认（字符串拼接非 p.join） |
| FutureBuilder 反模式 | ✅ 确认（StatelessWidget 重建即重查；无 hasError 分支） |
| #5 撤回（本机无顺序播放态） | ✅ 撤回维持：`_restorePlaybackMode` 只恢复三态、completed→回绕闭环无洞 |
| #11 撤回（SSDP 竞态不可达） | ⚠️ 措辞修正：`stopListening` 实际有调用点（`ssdp_discovery.dart:477` dispose → `dlna_manager.dart:1083`），但仅在 App 拆除时触发，运行期仍不可达 |
| frozen_playback / effective_playback 补录 | ✅ 确认（effective 行号应为 :29,43,54） |

**v2 的 6 条 🟠 全部经第三视角复核存活，撤回结论维持。**

## 3.2 第三轮新发现（前两轮盲区）

### 🟠 新增

| 位置 | 问题 | 说明 |
|---|---|---|
| `lib/data/sources/local_storage.dart:130-135` + `lib/data/models/server_config.dart:49-60` | **第二处凭据明文落盘点**：`saveServerConfig` 将含 password/apiKey 的 ServerConfig `jsonEncode` 进 SharedPreferences | 与 SQLite 明文同根，一次 `flutter_secure_storage` 改造可同时覆盖 |
| `lib/data/sources/subsonic_api_client.dart:406-411,366-401` | Subsonic `t/s` token **会话内复用且拼入所有 URL**（封面/流 URL 均带 `u/t/s`） | salt 固定 → token 改密前永久有效，等同密码等效凭据；http 下可重放并进服务端访问日志。协议固有设计，依赖 https；自建家庭 LAN 场景风险有界 |
| `windows/runner/desktop_lyric.cpp:74-158` | 桌面歌词窗**无 WM_DPICHANGED**（主窗 win32_window.cpp:359 有），固定 520×84 设备像素 | 高 DPI/跨屏拖动时悬窗偏小、字号不跟 |
| `lib/providers/player/player_provider.dart:1660-1668` | `_fadeOut` 会话中断分支 `setVolume(0.0)`，若新加载失败进入暂停且无后续 `_cancelFade`/`_startPlayback`，播放器音量残留 0（静音） | 快速切歌 + 新源加载失败路径；恢复依赖下一次 play 的 fade 链 |

### 🟡 新增

- `cast_peer_provider.dart:704-790`：轮询 `_tick` 有 peerId 守卫（:710,:750）但**无递增序号守卫**，同一 peer 旧响应可晚到覆盖新 playMode/castIndex（`toggle:357`/`setPlayMode:479` 乐观置位 + `unawaited(pollOnce())` 加剧）；轮询周期短会自愈，表现为偶发状态回跳。
- `desktop_lyric.cpp:141-154`：`TrackPopupMenu` 后缺 `PostMessage(WM_NULL)`（tray/main.cpp:61 有正确写法），右键菜单可能需额外点击。
- `desktop_lyric.cpp:173-176`：字体用 `DEFAULT_CHARSET`，非 CJK 字符集机器中文歌词可能方块。
- `audio_handler_service.dart:222-224`：`dispose()` 从未被调，热重启泄漏旧 AudioPlayer/handler（仅开发期）；stream 订阅未 cancel。
- `audio_handler_service.dart:149`：`updateMediaItem` 强制 `playing:true`，Android 通知播放态短暂失真。
- 全程无"http 明文连接"提示（address_pool:163 默认端口；subsonic_api_client:254-259 注释甚至建议 DLNA 用 http）。
- DLNA `/rest/dlna/stream/:token` 一次性语义完全信任服务端，客户端不校验（260-281）。

### 第三轮证伪

- ~~播放会话落盘可能含鉴权 token~~ → **证伪**：`Song.toJson`（song.dart:244-281）不含普通流 URL，仅 `previewStreamUrl`（试听源 URL，无 Subsonic 凭据）；普通歌 URL 由 `getStreamUrl` 动态生成不落盘。

### 第三轮确认无问题的方向

Win32 线程安全（全部 Handler 在 UI 线程）、GDI 对象配对、托盘通道分发、quit 落盘→DestroyWindow 顺序、
歌词窗热重启不重建（wWinMain 单次创建）、transcoded_stream_seek 边界、playback_payload 越界、
shuffle_queue_indexer、address_pool probeAll 去重、api_provider 编排无环、
local_storage 损坏 JSON 兜底（repairCorruptPreferences）。

## 3.3 v3 收敛后的修复优先级

1. **凭据存储统一改造**（SQLite + SharedPreferences 两处合并，flutter_secure_storage）+ 删除 `// Encrypted` 误导注释。
2. `device_description` finally-close + 读超时。
3. 离线缓存三件套：原子写 / init 扫描孤儿 / tmp 路径 `_safeName`。
4. `aggregate_search_results` future 提升 + hasError 分支。
5. `desktop_lyric` 补 WM_DPICHANGED + 字体 CHINESEBIG5→GB2312_CHARSET/DPI 自适应；`_fadeOut` 中断恢复音量。
6. Subsonic token（https 优先提示、URL 凭据进日志问题）作为后续安全项评估。
7. 🟡 批次：cast_peer 序号守卫、audio_handler 生命周期、WM_NULL、DEFAULT_CHARSET、playing:true。

**三轮总结论**：核心链路健壮性经三个独立视角交叉验证后仍然成立；真实问题收敛为
**凭据存储（2 处）、DLNA HTTP 资源管理、离线缓存文件安全、桌面歌词 DPI、crossfade 音量残留**
五大主题，无稳定复现的 🔴 崩溃。

---

# v4 · 第四轮（剩余区域全扫）

覆盖此前未审区域：library/media/auth providers 业务逻辑、播放器 UI 大文件（mini_player 2067 行 /
full_player_page 1782 行 / play_queue_sheet 929 行）、covers/lyrics 多源与模型解析、启动编排、
Android 原生侧。4 路并行 + 人工复核关键结论。

## 4.1 复核中证伪的子代理结论

- ~~`switchLibrary` 不落库导致下游不切换~~ → **证伪**：`app_drawer.dart:246-249`、
  `app_settings_page.dart:583-587` 两处调用方均先 `setActiveLibrary(library.id)` 落库再同步内存，
  下游 `activeLibraryProvider`（DB stream）正确切换。

## 4.2 确认的新问题

### 🟠 高价值

| 位置 | 问题 | 说明 |
|---|---|---|
| `lib/providers/auth/auth_provider.dart:32,188` + `:153-157` + `lib/app.dart:197` | **`isInitializing` 状态机漏态 → redirect 守卫死代码**：`AuthState` 默认构造 `isInitializing=true`；`logout()` 用裸构造误置 true；`_handleLoginResult` 的 copyWith 不含该字段 → **登出后再登录 isInitializing 恒 true**，`redirect`（:197 永远 return null）失效 | 登录/登出跳转由 `app.dart:172-185` 的 `ref.listen` 命令式 go 补偿，日常无感知；但"未登录访问受限路由被拦截"的守卫彻底失效。修复：logout/登录成功显式 `isInitializing:false` + GoRouter 加 `refreshListenable` |
| `lib/features/player/widgets/mini_player.dart:626-660` | **迷你条进度条拖动中切歌 → 松手 seek 落错位置**：拖动存比例，`onDragEnd` 用 `widget.duration * progress` 换算；拖动中歌曲切走则套到新歌时长上 | 全屏页有 `_dragSongId` 守卫（full_player_page:1109,1174,1185-1188）而 mini 没有，对比明确。修复：同款守卫 |
| `android/app/build.gradle.kts:24,32-35,76-82` | **release 签名不完整时静默回退 debug 签名**（硬编码 `Z:/echokey/keystore.jks`，缺任一凭据即 fallback） | CI 走 repo Secrets 时正常，但漏配/本机构建会产出 debug 签名的 release 且**构建不报错**——违反"发版必须 CI keystore"防线精神。修复：缺失时 error 中断 |
| `lib/providers/cast/dlna_provider.dart:98,541,632` | **Android 13+ 通知权限只在直投时申请**：`_requestBackgroundCastPerms` 不在本地播放路径调用 | 纯本地播放用户从未被请求 `POST_NOTIFICATIONS`，媒体通知可能被抑制。修复：首次本地起播即申请 |
| `lib/providers/media/lyrics_cover_provider.dart:143,192` | **离线歌词缓存 key 无 libraryId 隔离**：`cache.lyrics(song.id)` 只用 songId | 多库同 id 时歌词互相污染（对照 offline_cache_manager:346/351 确认 key 结构）。修复：key 拼 libraryId |
| `lib/data/models/song.dart:197-198` + `data/sources/metadata_cache_repository.dart:190,225,419,459` | **`id`/`title` 用 `as String` 且缓存读取无逐条容错**：一条缺字段数据抛 CastError → 整次缓存读取返回 null | 对比 `_parseCachedSongs:134-146` 有逐条容错，不一致。修复：`as String?` + 统一逐条容错 |
| `lib/data/sources/lyrics/structured_lyrics.dart:31`、`lyrics_line.dart:10` | **`offset`/`start` 用 `as int?`**：OpenSubsonic 常返回浮点（1.0）→ CastError，结构化歌词整组丢失（静默降级 legacy） | 修复：`as num?` 取整 |
| `lib/providers/library/library_stats_provider.dart:64` | **`ensureActiveAddressProvider.future` 在 try 外**：地址解析失败 → 四维度计数全空 | 修复：try/catch 降级 |
| `lib/providers/library/playlist_provider.dart` | **歌单列表/详情缓存无编辑失效**：增删歌后 `fetchWithCacheFallback` 可能回放旧列表 | 修复：mutation 路径显式失效磁盘缓存 |

### 🟡 低优先

- `lrc_parser.dart:7,34`：不剥离增强型逐字标签 `<mm:ss.xx>`，字面残留进歌词文本。
- `lyrics_cover_provider.dart:313-327`：当前时间早于首行时抢显第一行（应返回 null）；启用时 ~200ms 重算。
- `random_songs_push_provider.dart:65-116`：重连用单一 `_connecting` 标志，地址变更瞬间并发连接可丢一次重连。
- `recommend_provider.dart:161-175`：随机池仅前 4 页（≤400），大库随机不完整。
- `song_options_sheet.dart:518-523`：加入歌单无重复添加防护。
- `play_queue_sheet.dart:733`：投屏队列仍用已废弃 `onReorder(int,int)` 签名（SDK 升级即编译失败）；本地队列（:894）不支持拖拽排序，能力不一致。
- `MainActivity.kt`：无 onDestroy 释放 multicastLock/wakeLock；`pendingNearbyResult` 单槽，连续权限申请覆盖首个 Result。
- `update_checker.dart:166`：版本比较正确（数值），但 `1.2.3-beta` 预发布段不健壮；`:132` 下载 URL 未校验 scheme/域名。
- `cover_repository.dart:33`、`lyrics_repository.dart:26`：多源**串行**回退（每源 10s 超时，最坏数十秒）且 LrcParser 主 isolate 解析。
- `metadata_cache_repository.dart:336`：`clearPlaylistCaches` 直接 `SharedPreferences.getInstance()` 绕过损坏容错。
- `AndroidManifest.xml:39-40`：LAUNCHER activity `taskAffinity=""`，深链可能落入他应用任务栈（确认是否有意）。
- `audio_handler_service.dart:149`：（v3 已录）`updateMediaItem` 强制 playing:true。

### 4.3 第四轮确认无问题的方向

- 搜索防抖用 `SearchRequest` 作 family key，序列安全；windowed 分页游标无错位；
- mini↔full 三态（本机/链路A/链路B）分支无漏，全仓无按钮绕过 `effective_*` 包装直调本地 next/previous；
- 全屏 scrubber 拖动竞态防护完整（`_dragValue` + `_dragSongId`）；Hero tag 成对一致；黑胶无 Ticker；
- 封面/歌词回退链不缓存"网络错误为无结果"；URL 编码完整（encodeComponent / queryParameters）；
- 升级版本比较数值正确（1.10 > 1.9）；MainActivity channel 全主线程、result 配对正常；main.dart 启动编排无首帧竞态。

## 4.4 四轮总收敛

新增修复主题：**认证状态机**（isInitializing 漏态 + refreshListenable）、**歌词/模型解析容错**
（as int/String + 逐条容错 + libraryId 隔离）、**迷你条 seek 守卫**、**通知权限**、**签名防呆**。
与 v3 五大主题合并后，按价值排序的前 10 项：

1. 凭据存储统一改造（SQLite + SharedPreferences，flutter_secure_storage）
2. `isInitializing` 状态机 + GoRouter refreshListenable
3. `device_description` finally-close + 读超时
4. 离线缓存三件套（原子写 / 孤儿回收 / tmp `_safeName`）
5. 歌词离线缓存 key 加 libraryId
6. song/structured_lyrics 解析容错（as num/String? + 逐条容错）
7. mini_player 拖动切歌守卫（对齐全屏页）
8. Android 通知权限本地播放路径
9. `aggregate_search_results` future 提升 + hasError
10. release 签名缺失时中断构建

---

# v5 · 第五轮（复核 v4 问题清单）

用户要求重新审计 v4 结论。方法：v4 的 9 条 🟠 全部亲自读源码/调用链/CI workflow 验证，
不再经子代理转述。**结论：4 条确认、4 条降级、1 条撤回；另发现 2 处路径错误。**

## 5.1 v4 九条 🟠 逐条复核

| v4 # | v4 结论 | v5 复核 | 依据 |
|---|---|---|---|
| 1 | `isInitializing` 漏态 → redirect 死代码 🟠 | ✅ **确认** | auth_provider:32 默认 true / :188 logout 裸构造（`AuthState(isAuthenticated: false)` 其余默认→isInitializing=true）/ :153-157 登录成功 copyWith **调用点未传** isInitializing（copyWith 本身支持该字段 :40,47，v4/v5 曾误写"不含"）；app.dart:197 redirect 恒 null。命令式 ref.listen（app.dart:172-185）补偿主跳转，日常无感但守卫规则面失效 |
| 2 | mini_player 拖动中切歌 seek 错位 🟠 | ✅ **确认** | mini_player:647-660 `onDragEnd` 用**当时** `widget.duration × 存的比例`，无 songId 校验、无 ValueKey；对照 full_player_page:1109/1174/1185-1188 有 `_dragSongId` 守卫 + scrubber `ValueKey(songId)`（:1144，切歌即重建）。mini 无 key → State 复用 → 拖动中切歌松手落错位置 |
| 3 | release 签名静默回退 debug 🟠 | ✅ **确认并补强** | build.gradle.kts:76-82 无凭据回退 debug 属实；补查 CI：build-android.yml:132-136 Decode step **无条件**，secret 为空时 `base64 -d` 静默产空文件 → gradle `hasReleaseSigning=false` → **debug 签名且 job 绿**。当前 Secrets 正常故产物无碍，但漏配/误删会静默产出不可覆盖安装的包，且用户现有验证手段（uploader==github-actions[bot]）**无法发现签名变化** |
| 4 | Android 13+ 通知权限只在投屏申请 🟠 | ✅ **确认（方向修正后更实锤）** | audio_handler_service.dart:1-9 用 `audio_service` BaseAudioHandler——**本地播放也有媒体通知**（注释"后台播放和通知栏控制"）；POST_NOTIFICATIONS 声明于 Manifest:19；request 仅在 dlna_provider:542/:632（cast 路径）。纯本地播放用户 → 权限 never asked → 通知栏媒体控制/锁屏不可用 |
| 5 | 歌词缓存 key 无 libraryId 🟠 | ⚠️ **代码属实，降 🟡** | offline_cache_manager:352 key=`lyric:<songId>`（:346-348/:351-357 只收 songId）；写入点 lyrics_cover_provider:188-192（在线播放即写）。触发需「多库 + id 撞车 + 离线读」三条件交集。**修复极廉价（key 拼 libraryId）**，保留为低成本确定性项 |
| 6 | song.dart `as String` + 缓存无逐条容错 🟠 | ⚠️ **代码属实，影响修正，降 🟡** | song.dart:197-198 `as String` 属实；metadata_cache_repository 逐条容错仅 `_parseCachedSongs`（:134-146，只用于 random songs :126），getAllSongs:190/getAlbumDetail:224 等 6 路直接 `.map(fromJson)`。**兜底并不完整**（v6 勘误）：music_provider 5 处 cacheRead 走 fetchWithCacheFallback（:53-60 有 try/catch，不崩）；但 **playlist_provider 3 处缓存直读无兜底**（:54、:143、:257，坏缓存+网络失败时 :257 在 catch 内再抛 → FutureProvider error 态而非崩溃）；song_list_page:213-225 直调 repository 在 try 内。自写自读缓存坏数据概率低。⚠️ **v4 路径错**：实际在 `data/repositories/` 非 `data/sources/` |
| 7 | structured_lyrics `as int?` 🟠 | ⚠️ **成立，描述修正，降 🟡** | ⚠️ **v4 路径错**：实际 `data/models/structured_lyrics.dart:31`（offsetMs）+ `data/models/lyrics_line.dart:10`（startMs）。"OpenSubsonic 常返回浮点"**无依据**（规范为整数）；但**字符串数字同样 CastError**——song/artist/album 的 `_toInt` helper（song.dart:8 等）均兼容 String，反证服务端数值类型不稳。触发依赖服务端实现（自建后端可控）；失败被 subsonic_lyrics_source:40-58 try/catch 静默回退 legacy，用户无感。低成本改 `as num?`/兼容处理 |
| 8 | library_stats `ensureActiveAddress` 在 try 外 🟠 | ❌ **撤回** | ensureActiveAddressProvider（api_provider:50-91）**设计为有配置即不抛**（:76-84 best-effort 回退），仅"完全无配置"throw；而 libraryCountsProvider:61 `repository==null`（无活跃库）**先 return 空**——throw 需"有活跃库但地址池空"的异常态，几乎不可达。即使 throw：4 个消费端（artist_list:63-65 等）全用 `.maybeWhen(data:, orElse:'')` → 子标题留空，页面主体走独立请求不受影响。可感知影响≈无 |
| 9 | playlist 缓存无编辑失效 🟠 | ⚠️ **降 🟡** | mutation 路径已有完整失效链：song_options_sheet:524-525 加歌后 invalidate `playlistsProvider`+`playlistDetailProvider(id)`；playlist_detail_page:748-760 删歌后 invalidate + 重拉。fetchWithCacheFallback 远程成功即写新缓存（:37-41）→ 磁盘缓存自动刷新。残余窗口仅「mutation 成功 → invalidate → 重拉前断网 → 回退旧缓存」，缓存兜底系统固有、影响轻微 |

## 5.2 v4 🟡 抽查

- `lrc_parser.dart:34` 只剥 `[mm:ss.xx]` 方括号标签，增强型 `<mm:ss.xx>` 逐字标签残留 ✅ 属实。
- `play_queue_sheet.dart:733` onReorder 净索引由组件封装处理（:734-736 注释明确）——「SDK 升级即编译失败」**未证实**（当前 0 error 编译通过），需具体 deprecated API 佐证后才算问题。
- `AndroidManifest.xml:40` `taskAffinity=""`：无深链 intent-filter（仅 MAIN/LAUNCHER），实际无行为影响，可不动。

## 5.3 v5 收敛

**v4 方向经受住考验**：4/9 条 🟠 确认，且恰是用户可感知/防线相关项；5 条定级偏高被校准
（其中 #8 撤回）；无新误报反向翻转。跨 5 轮审计累计：核心链路健壮，真实问题收敛为
**签名防线、认证状态机、迷你条 seek 守卫、通知权限、凭据存储、DLNA HTTP 资源、离线缓存文件安全、
解析容错**——无 🔴 级稳定复现崩溃。

**v5 修订后的修复优先级**：
1. 签名防线（#3）：gradle release 缺凭据 **error 中断**（不要 debug 回退）+ workflow 显式校验 secret 存在
2. `isInitializing` + GoRouter refreshListenable（#1）
3. mini_player seek 加 songId 守卫 + `ValueKey(songId)`（#2，对齐全屏页）
4. 本地播放首启申请 POST_NOTIFICATIONS（#4）
5. 🟡 批次：歌词缓存 key 拼 libraryId（#5）、song/structured_lyrics 解析容错（#6/#7）、
   缓存读取逐条容错推广（#6）、凭据存储统一、device_description finally-close、离线三件套

---

# v6 · 第六轮（复核 v5 自身）

对 v5 的裁决做第二轮自我审查，重点攻击 v5 论证中未闭合的缺口。**结论：v5 全部裁决维持，
但修正 2 处依据错误、补强 2 处论证。**

## 6.1 修正的依据错误（结论不变）

1. **#1 的依据表述**：v4/v5 写"_handleLoginResult 的 copyWith 不含 isInitializing 字段"——
   **错**。`copyWith` 明确支持该字段（auth_provider:40,47）。真实链路是：
   `logout()` :188 用 `AuthState(isAuthenticated: false)` 裸构造（isInitializing 回落默认 true）→
   登录成功的 copyWith **调用点**（:153-153-157）未传该字段 → true 被保留 → redirect 死。
   结论与修复方案不变，但机制描述需以此为准。
2. **#6 的影响表述**：v5 写"坏数据不崩、只离线空块"——只对走 `fetchWithCacheFallback` 的
   **music_provider 5 处**成立。`playlist_provider` 3 处缓存**直读**（:54、:143、:257）无内层
   兜底，坏缓存 + 网络失败时 :257 在 catch 块内再抛 → FutureProvider 进入 error 态（UI 有
   failedProvider 分支，非崩溃）。定级 🟡 不变，"逐条容错推广"修复范围应包含 playlist 直读路径。

## 6.2 补强的论证

- **#2**：追完 progressLayer 全链——mini_player:79 `const _ProviderMiniPlayerProgress()` 无 key、
  :575 直接渲染无 key，`_MiniPlayerViewState` 亦无随歌曲变化的重建机制 → 拖动态 `_scrubProgress`
  跨歌曲保留实锤，#2 无误报反转。
- **#3**：build-android.yml **全文无任何 secret 非空校验**（grep 证实），Decode step 空输入
  `base64 -d` 静默成功 → v5 补强成立。防线修复点两处：gradle error 中断 + workflow 校验。

## 6.3 v6 后的最终清单（与 v5 一致，仅依据修正）

签名防线 → isInitializing（logout 显式置 false / 调用点传参）→ mini seek 守卫 → 通知权限 →
🟡 批次（歌词 key、解析容错、playlist 直读容错、凭据存储、device_description、离线三件套）。

**六轮审计正式收敛。**后续不再重复审计，直接按清单修复。
